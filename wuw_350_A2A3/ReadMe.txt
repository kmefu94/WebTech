5. Memorizing user input

The user input was stored into a string with the variable $phrase retrieved using $_POST to pull it from the textbox with the 'phrase' name. This string would then be split by whitespace to separate each unique word.

To reach the first page of A2 - http://localhost/wuw_350_A2A3/A2

To reach first page of A3 - http://localhost/wuw_350_A2A3/A3